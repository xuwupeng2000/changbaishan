= smart_listing_controls_for(:customers) do
  .filter.input-append.search-inline-form
    = text_field_tag :filter, '', class: "form-control", placeholder: "Search name, address, or id here", autocomplete: "off"
    %button.btn.btn-success{type: "submit"}
      = icon 'search'

- unless smart_listing.empty?
  %table.table.table-hover.table-condensed
    %thead
      %th= smart_listing.sortable "Name", :name
      %th Address
      %th Customer ID
    %tbody
      - smart_listing.collection.each do |customer|
        %tr
          %td= customer.name
          %td= customer.address
          %td= customer.id


  .pagination
    = smart_listing.paginate
    = smart_listing.pagination_per_page_links
- else

  .clearfix

  %p.warning.sl-no-record No records!
