= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form-horizontal', method: :put}) do |f|
  = f.error_notification

  .col-lg-12
    = f.input :email, required: true, input_html: { tabindex: 1 }

  .col-lg-6.pull-left
    = f.input :first_name, autofocus: true, required: true, input_html: { tabindex: 2 }
    = f.input :password, autocomplete: "off", hint: "leave it blank if you don't want to change it", required: false,  input_html: { tabindex: 4 }

  .col-lg-6.pull-right
    = f.input :last_name, required: true,  input_html: { tabindex: 3 }
    = f.input :password_confirmation, required: false, input_html: { tabindex: 5 }

  .col-lg-12
    = f.input :current_password, hint: "we need your current password to confirm your changes", required: true, input_html: { tabindex: 6 }

  .col-lg-12
    .form-actions
      = f.button :submit, "Update"
      = link_to "Back", :back, class: 'btn btn-default'
